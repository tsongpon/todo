todo:
     image:   tsongpon/todo:${BUILD_NAME}
     mem_limit: 1073741824
     ports:
        - "8090:8090"
     environment:
        DB_HOST: ${DB_HOST}
        DB_USER: ${DB_USER}
        DB_PASSWORD: ${DB_PASSWORD}